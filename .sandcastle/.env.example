# Required for Sandcastle's GitHub Issues workflow.
# Create a fine-grained token with repository Metadata (read) and Issues (read/write).
GH_TOKEN=

# Required by Pi for the selected model/provider.
# Use a dedicated, user-controlled key. Never commit the real .env file.
OPENAI_API_KEY=
